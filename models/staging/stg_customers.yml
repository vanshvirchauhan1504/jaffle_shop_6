version: 2

models:
  - name: stg_customers
    description: "Cleaned customer data from the raw customers table."
    columns:
      - name: customer_id
        data_tests:
          - not_null
          - unique